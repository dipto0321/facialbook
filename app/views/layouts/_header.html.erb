<nav class="navbar navbar-expand-lg navbar-dark bg-primary bg-fb nav-height">
  <a class="navbar-brand" href="<%= root_path %>" >Facial Book</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <% if user_signed_in? %>
      <%= render  "layouts/header_profile" %>
    <% elsif params[:controller] != "devise/sessions" %>
      <%= render  "layouts/header_form" %>
    <% end %>
  </div>
</nav>